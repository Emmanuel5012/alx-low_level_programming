#include "main.h"
/**
 * print_alphabet - function to print abc
 *
 * Return: 0
 */

void print_alphabet(char)
{
char alpha;

alpha = 'a';

while (alpha <= 'z')
{
_putchar(alpha);
alpha++;
}
_putchar('\n');
}
